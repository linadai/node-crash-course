// const {names, age} = require('./people.js'); //include people.js and run people.js
// console.log({names, age});


const os = require('os'); //require the os module from the built-in node module; os is a built-in module
console.log(os.platform()); //check the computer's operating system